# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Forge dev runs
run/
run-data/
runs/

# Generated resources are committed on purpose: the shipped lexicon lives in
# Lexicon.java and `runData` is what turns it into data/epigraphy/glyphs/*.json,
# so the JSON has to be in the tree for anyone reading the pack without building it.

# IDE
.idea/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
.vscode/
bin/
out/

# OS
.DS_Store

# Datagen hash cache — regenerated by runData, not content
src/generated/resources/.cache/
